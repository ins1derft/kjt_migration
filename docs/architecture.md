# Архитектура монорепозитория

Монорепо объединяет публичный фронтенд на Next.js 16 (App Router) и бэкенд на Laravel 12 с админкой MoonShine 4 и PostgreSQL 18. Трафик всегда проходит через Nginx; оркестрация только через `docker/compose.sh`.

## Поток запросов (runtime)
```
Браузер → Nginx (:8080 dev / :80 prod)
  ├─ /admin, /api, /build, /storage, /vendor → php-fpm (backend-php) → PostgreSQL
  └─ остальное (/,_next, статические) → Next.js (frontend)
Next.js при загрузке данных дергает /api/* (через Nginx) → Laravel → PostgreSQL
```

## Фронтенд (директория `frontend/`)
- Next.js 16, App Router, SSR. Стилизация — TailwindCSS (utility `cn` в `src/lib/utils.ts`). Дизайн‑система под новый лендинг: Open Sans + Lorin, палитра brand.sky/orange/gradient start‑mid‑end, `shadow-glow`, `bg-brand-gradient`, новые анимации/токены в `app/globals.css` и `tailwind.config.js`. Safelist в `tailwind.config.js` генерирует `pt-[0..600px]`/`pb-[0..600px]` и их responsive-варианты (`sm:`/`md:`/`lg:`) под произвольные паддинги из CMS.
- Главные маршруты:
- `/` (`app/page.tsx`) — лендинг теперь тоже приходит из сущности Pages: сервер тянет `/api/pages/home` и рендерит блоки через общий реестр. Если `type=product_landing` и в блоках нет `product_hero`, перед блоками добавляется ProductHero из связанного продукта. Формы подтягиваются только из `product.form` и блоков `product_hero` (`formsByCode`). При отсутствии записи slug `home` выводится мягкий плейсхолдер для админа. Данные для существующего лендинга миграцией `2025_12_03_000100_seed_home_page_from_static` автоматически создают опубликованную страницу `slug=home` с теми же блоками, что были в статике.
- `(marketing)/[slug]` — либо продуктовые лендинги (`type=product_landing`), либо статические страницы (`type=static`), данные с `/api/pages/{slug}`, блоковый рендер через реестр. Для `product_landing` дополнительно прилетает `product`, `variants`; перед блоками жестко рендерится `ProductHero` из продукта **только если** в блоках нет layout `product_hero` (чтобы избежать дубляжа). Конфиги форм подтягиваются на сервере для всех кодов, встреченных в продукте и в блоках `product_hero` (map `formsByCode` → передается в рендерер); QuoteModal работает без клиентского GET. Новый layout `product_hero` позволяет заполнить поля вручную и не опираться на Products API (`useProductData=false`), выбирать вариант бейджей (`badgeVariant=image|card`), задавать только изображения + подпись (без icon key); CTA показывается при наличии `formCode`, даже если страница статичная и продукта нет; если `useProductData=true` и продукта нет, CTA скрывается. При желании можно оставить автозаполнение из `product` (`useProductData=true`). Games блоки берут игры из сущности `Game` (авто — все/limit, ручной — выбранные slug’и из выпадающего списка).
  - `/news` и `/news/[...slug]` — список статей `type=news` и детальная статья по slug последнего сегмента.
  - `/case-studies` — список `type=case_study`, карточки ведут на `/news/{slug}`.
  - `/games` и `/games/[slug]` — каталог игр и детали.
- `/sensory-room/[slug]` — детальная страница Sensory Room Bundle; SSR тянет `/api/sensory-room-bundles/{slug}` и рендерит `PageHeader` с breadcrumbs + контент `SensoryRoomBundleDetailClient`.
- `/store` и `/store/[slug]` — каталог и карточка StoreProduct.
- `/case-studies` — список кейсов (articles type=case_study) на Tailwind/shadcn `Card`.
- Конфиг: `next.config.mjs` (`output: "standalone"`). В dev переписывает `/api/*` на `http://nginx/api/*` или `NEXT_API_PROXY`; в прод переписываний нет.
- Глобальный каркас `app/layout.tsx` выводит шапку/футер (`src/components/layout/SiteHeader|SiteFooter`), поднимает меню `header`/`footer` из `/api/menus` (helpers `src/lib/menus.ts`, revalidate 300s) **и единый объект настроек сайта** из `GET /api/site-settings` (`getSiteSettings` в `src/lib/api.ts`). Header — двухуровневый sticky (40px синий топ-бар + 60px белый основной), контейнеры `max-w-[1189px]` (2xl `1320px`, без внутренних паддингов на 2xl) с центральным слотом под логотип 110×100; нижний ряд — сетка `1fr/auto/1fr` с адаптивными зазорами: `48/80/120px` слева и `50/80/95px` справа (переключение на мобильный режим при ширине <1000px). Mega‑menu рендерится для первого пункта с `children` (по данным API), привязки к тексту нет. Мобильный дровер: стрелка показывается только у пунктов с вложениями; иерархия дочерних пунктов выводится двумя уровнями (группа → элементы). Footer — полностью динамический: навигационные колонки строятся из меню `footer` (слот `footer`), соцссылки/контакты/поддержка приходят из настроек. Миграция `2025_12_09_150500_seed_footer_menu_items` создаёт структуру футера «Catalog» / «Helpful Links» и потомков (interactive-* и news/case-studies/store/contact), чтобы админка MoonShine управляла тем же набором ссылок без жёсткой верстки. Палитра/градиенты задаются в `globals.css`.
- SEO-база: переехали на `next-seo` + `next-sitemap`. `src/lib/seo.ts` конвертирует конфиг `next-seo.config.ts` (titleTemplate, OG/Twitter, canonical из `NEXT_PUBLIC_SITE_URL`) в `Metadata`; `DefaultSeo` подключён в `app/layout.tsx`. Динамические страницы собирают `Metadata` через `mergeSeo`/`nextSeoToMetadata`. JSON-LD остаётся для статей (NewsArticle), игр (VideoGame) и товаров магазина (Product). robots/sitemap теперь генерируются postbuild через `next-sitemap` (конфиг `next-sitemap.config.js` с выборкой статичных путей + articles/games(is_indexable)/store products из API); статические файлы лежат в `public/` после билда, отдельные маршруты `app/robots.ts`/`app/sitemap.ts` удалены. Стили — брендовые HSL-токены в `app/globals.css`, Tailwind config `tailwind.config.js`, postcss конфиг `postcss.config.js`.
- Переменные окружения, доступные клиенту: `NEXT_PUBLIC_API_URL` (по умолчанию `http://localhost:8080/api`; в прод‑компоузе прокидываем `http://nginx/api`, чтобы SSR ходил к Laravel через Nginx внутри сети compose), `NEXT_PUBLIC_SITE_URL` (для `next-seo`/`next-sitemap`; fallback `https://kidsjumptech.com`), опционально `SITEMAP_API_BASE` для генерации sitemap.
- Оптимизация медиа: `resolveMediaUrl` строит абсолютные ссылки для Next Image через `NEXT_IMAGE_BASE_URL` (dev: `http://nginx`, внутренний хост в сети compose; fallback — `NEXT_PUBLIC_SITE_URL`). В блоке `hospital_equipment` все изображения (иконки фич, фон CTA, футерный значок) рендерятся через `next/image` с `unoptimized` и `resolveMediaUrl`, чтобы сохранить контроль размеров/overlay и избежать ошибок `_next/image` при временной недоступности файлов. Анимация градиента теперь включается только по hover через класс `animate-gradient-hover` (utility в `app/globals.css`) на кнопках с градиентным фоном.
- Блоковый рендерер: типы → `src/lib/blocks/types.ts` (перенесены из прежнего design), реестр `src/lib/blocks/registry.tsx` рендерит блоки новым набором дизайн-компонентов (Hero, HeroValueGrid, FeatureGrid, **FeatureGridIntro**, **CustomSoftwareDevelopmentIntro**, ProductHero, ProductCarousel, GamesGallery, **GamesGrid**, Research, ResearchResults, **ExclusiveOffer**, News, Stats, FAQ, WhyUs, ProductDescription, ProductNav, OurApproach, ProductSpecs, CompareModels, CTASection, **CustomSoftware**, **DiscountBanner**, HighlightCTA, Reviews, TrustedBy, SpecialNeeds, HospitalEquipment, PotentialUses, AppreciationLetters, TeamGrid, ContentHighlight, GameDistribution, PracticeShowcase, SensoryRoomBundles) из `src/components/blocks`. При рендере каждый блок оборачивается в `div id=<anchor>` с автогенерацией якоря: приоритет `values.anchor` → пресет по имени лэйаута (hero/description/specs/compare/approach/faq/cta/etc) → slug заголовка → slug имени лэйаута; slugifier транслитерирует кириллицу и чистит не‑ASCII, дубликаты получают суффиксы `-2`, `-3`. Формат данных API прежний `{ name, key, values }`, маппинг на новый дизайн идёт по `name`. Legacy/shadcn блоки удалены; поддерживаются только текущие компоненты. В админке для всех layout одно текстовое поле `padding` (строка с классами/inline-стилями Tailwind) + опциональный `backgroundColor` (Color picker с HEX) для переопределения фона; при пустом значении фон берётся из дефолтного класса компонента. Числовых `padding.top/bottom` больше нет.
- Новый layout `potential_uses` (frontend `PotentialUses`) для страницы `hospital-equipment`: белый верх с заголовком 64px (38px на 360px), `font-heading` 700, `leading-1.05`; таб-группа с закруглением 100px и тенью `0 1px 10px rgba(0,0,0,0.05)` у активной кнопки, текст табов #4f5459. Габариты таб-бара: 954×87 на 1920, 954×87 на 1189, 320×97 на 360 (две строки по 2 таба; отступ до карточек 50px/50px/74px). Карточки: при 1920 — ширина 569px, высота 260px, gap 36px, общий контейнер 1174px; при 1189 — 519×260, gap 51px, контейнер 1089px; при 360 — 320×160, один столбец. Тайтлы карточек `font-heading` extraBold, 24px (16px мобайл), цвет **#4571ff** (токена нет); описания `font-sans` 20px (16px мобайл) `leading-1.4`, цвет `text-brand-dark/70`. Поля блока: `title`, `tabs[{ key, label, description, cards[{ image, title, titleHref?, description }] }]`, `padding`. Дефолтные изображения лежат в `frontend/public/images/hospital-equipment/potential-floor.jpg` и `potential-table.jpg` (путь в данных начинается с `/images/...`). Миграция `2025_12_07_000400_seed_potential_uses_block` добавляет блок первым на страницу `slug=hospital-equipment`.
- Layout `gradient_form_banner` (frontend `GradientFormBanner`) поддерживает два шаблона: `classic` (текущая компоновка: слева иконка, текст и кнопка под ним, фон градиент `linear-gradient(90deg,#FFAD81→#A2B4FF)`) и `wide` (новый макет из дизайна: широкий градиент `linear-gradient(100deg,#FFB68D→#E2BCD5→#A2B4FF)`, текстовый столбец слева шириной до 740px, CTA‑кнопка справа, прижатая к нижнему краю колонки; иконка, если задана, рендерится в маленьком прозрачном скруглённом тайле). Поля: `title` (required), `description` (TinyMCE, nullable), `formCode` (Select из Forms, required; используется для модалки QuoteModal), `formTitle` (override заголовка формы), `ctaLabel` (default Consultation), `icon` upload (`pages/gradient_form_banner/icons`), `template` select `classic|wide` (default `classic`), `padding`, `backgroundColor`/`backgroundClass`. При пустом `formCode` CTA дизейблится, модалка не открывается.

- Layout `cta_section` (frontend `CTASection`) теперь умеет открывать QuoteModal по форме из Forms. Поля: `title`, `description` (RichText), `ctaLabel` (текст кнопки при наличии формы; default Contact us), `ctaHref` (fallback ссылка, применяется когда формы нет), `ctaHrefLabel` (текст кнопки в режиме ссылки; если пусто — берём `ctaLabel`), `formCode` (Select Forms; при заполнении кнопка открывает модалку и href не используется), `formTitle` (override заголовка формы), `backgroundImage`, `textColor`/`textColorClass`, `padding`, `backgroundClass`/`backgroundColor`. В реестре блоков код формы подгружается из `formsByCode`, поэтому на странице маркетинга добавлены загрузки formCode из `cta_section`.

- Layout `sensory_room_bundles` (frontend `SensoryRoomBundles` + `SensoryRoomBundlesListClient`) — список Sensory Room Bundles по Figma 1920/1189/360 (node 159:10813 / 159:14494 / 159:14515): фон `brand.gray`, внешний контейнер `px-5` (mobile, 360→320) / `lg:max-w-[1189px] lg:px-[50px]` (tablet) / `2xl:max-w-[1320px] 2xl:px-0` (desktop). Заголовок: 38px (mobile/tablet) → 64px (desktop, `2xl`), `font-heading` bold, `leading-none`, `text-brand-dark`. Описание: 16px (mobile/tablet) → 20px (desktop, `2xl`), `font-heading`, `leading-1.4`, `text-brand-dark/70`, `whitespace-pre-wrap`. Карточки грузятся SSR из `GET /api/sensory-room-bundles` (только published) и рендерятся вертикальным списком с разделителем между карточками (`divide-y divide-ui-dot`, отступы до линии по 50px сверху/снизу): на mobile — увеличенная галерея сверху (меньше top‑offset, `h-[320px]`, ширина `w-full` внутри карточки), затем контент и кнопки столбцом; на tablet/desktop карточка имеет `lg:min-h-[484px]` (высота не фиксирована и растёт под контент). На tablet/desktop — двухколоночная карточка (паддинги слева 35px, справа 20px tablet / 53px desktop; изображение справа 500×416 tablet / 582×484 desktop). Галерея — слайдер как в `TrustedBy`: drag-to-swipe (mouse/touch) через `translateX` + плавная анимация и кнопки prev/next (без dots), изображения через `next/image` (`unoptimized`). Заголовок карточки — ссылка на `/sensory-room/{slug}/` (детальная страница). `excerpt` хранится как HTML (TinyMCE) и выводится как RichText с выравниванием под макет. Блок `Includes` — список привязанных `products` с underline‑ссылками на `/{product.slug}/`, где `product.slug` — slug связанной страницы из `Pages` (`type=product_landing`, `status=published`) по `pages.product_id`, а не `products.slug`. Кнопки: `Custom Bundle` ведёт на `custom_bundle_url` (произвольная ссылка), `Live Demo` открывает `QuoteModal` по `form_code`, при этом заголовок/`topic` в модалке переопределяется заголовком bundle (чтобы lead сохранялся с темой bundle). Layout добавлен в Pages admin (`PageFormPage`) и вставлен на страницу `sensory-room` **между двумя** `cta_section` миграцией `2025_12_13_200000_add_sensory_room_bundles_block_to_sensory_room_page`.

- Layout `contact_form` (frontend `ContactForm`) — инлайновая форма по Figma 1920/1189/360: внешний контейнер до 1320px (`.container`, `px-5 md:px-6 2xl:px-0`), фон `#F4F5FA`, радиус 10px, тень `0 2px 20.6px rgba(0,0,0,0.05)`, внутренние отступы `px-[15px] py-[24px]` (base) и `px-[30px] py-[30px]` (md+). Заголовок 38/44px `font-heading` bold и описание 16/20px `font-sans` `text-brand-dark/70` выровнены влево, отступ до формы ~30px. Поля: label 18px `font-heading` extraBold, инпуты 50px, радиус 5px, бордер `#C6CBDF`, фон белый, `gap-[10px]`; телефон использует стиль `.phone-input-compact` (фон белый, префикс на `#F4F5FA`). Грид полей: base — 1 колонка, `lg` — 3 колонки с `gap-x-[24px]`, `xl` — `gap-x-[75px]`; чекбоксы 35px (радиус 5px, круг при ≤3 опциях), сетка опций `md:grid-cols-2`, `lg:grid-cols-3`, `xl:grid-cols-4` с `gap-y-[14px]`. Textarea min-height 100px. Дисклеймер (RichText) берётся из `Forms.config.disclaimer` (API `GET /api/forms/{code}`); если пустой — используется дефолтный текст в компоненте; выводится перед кнопкой, 16px `text-brand-dark/70`. Кнопка: 53px высотой, min-width 158px, радиус 100px, фон `#1A1A1A`, текст 16px `font-heading` bold белый. Поля блока: `title`, `description`, `formCode` (Select Forms), `padding`, `backgroundColor`. Добавлен на страницу `sensory-room` между второй `cta_section` и первым `feature_grid` (миграция `2025_12_13_150000_add_contact_form_to_sensory_room_page`).
- Новый layout `icon_title_text` (frontend `IconTitleText`) — универсальный центрированный текстовый блок по Figma (node 2051:29): иконка 36×36.227px + заголовок 24px `font-heading` ExtraBold, gap между иконкой и заголовком 9px; ниже описание 16px `leading-1.4` `text-brand-dark/70`, max-width 952px, `text-center`. Секция использует контейнер `.container` (`px-5 md:px-6 2xl:px-0`), фон по умолчанию `bg-brand-gray`, паддинги по умолчанию `pt-[70px] pb-[70px]` (override через `padding`). Поля блока: `icon` (Image upload → `public/pages/icon_title_text`), `iconAlt`, `title`, `description` (TinyMCE/RichText), `padding`, `backgroundColor`.
- Новый layout `counter_showcase` (frontend `CounterShowcase`) — секция с заголовком/описанием и большим счётчиком по Figma 1920/1189/360 (node 159:10838 / 159:15744 / 159:15781): фон `brand.gray`, паддинги по умолчанию `pt-[104px] pb-[124px] md:pt-[181px] md:pb-[180px]` (override через `padding`). Заголовок: 38px (mobile) → 64px (md+), `font-heading` bold, `leading-none`, `text-brand-dark`, max-width 320/974px. Описание: 16px → 20px, `font-heading` regular, `leading-1.4`, `text-brand-dark/70`, max-width 320/1051px. Счётчик: значение анимируется (паттерн `Stats`, 2s ease-out при появлении в viewport), градиент `bg-brand-gradient`, размер 207.267px → 384.495px; суффикс `+` (если есть в `value`) выводится отдельным элементом с размерами 45.281px → 84px и позиционированием по макету; подпись (label) 18.328px → 34px, цвет #656565. Опциональный CTA: при выборе `formCode` появляется кнопка под label (Figma node 2058:30), стиль `bg-gradient-cta` (334×53, radius 100px) и открывает `QuoteModal`. Поля блока: `title`, `description`, `value` (строка вроде `20+`), `label`, `formCode` (Forms.code), `formTitle` (override заголовка модалки), `ctaLabel`, `padding`, `backgroundColor`.
- Новый layout `appreciation_letters` (frontend `AppreciationLetters`) для списка новостей/писем благодарности с таб‑фильтрами по Figma 1920: общий фон белый, внутренний контейнер шириной 1320px, радиус 20px, фон `brand.gray`, паддинги панели `pt 104px / pb 92px / px 46px` на десктопе (мобайл 88/72/20→46 по брейкпоинтам). Заголовок 64px `font-heading` bold (38px на мобайле) цвет `brand.dark`. Табы (обязательное chessboard-переключение фильтров): группа 356×87px, радиус 100px, фон `brand.gray`; кнопки 161×65px, `font-heading` extraBold 20px, цвет `table.text` (#4f5459), активная — фон white + тень `0 1px 10px rgba(0,0,0,0.05)`. Контент: 3 элемента по умолчанию, чередование картинка/текст; изображение 598×326px, radius 10px, gap между колонками 54px, текстовая колонка с `pt 50px`; дата `font-heading` 16px `text-brand-dark/50`, заголовок 24px bold `text-brand-dark`, описание 20px `font-sans` `text-brand-dark/70`, расстояния date→title 16px, title→desc 20px, вертикальные разделители по `border #e1e4eb` с отступами 50px сверху/снизу. Поля блока: `title`, `query.limit` (default 3), `tabs[{ key, label, limit?, items?[slug[]], filters[{key,value}] }]`; если `items` заполнен, блок грузит ровно указанные статьи в заданном порядке (приоритет выше фильтров/limit). Filters конвертируются в `filter[<key>]` при запросе `/api/articles` (поддерживаются white‑листовые ключи `type|category|slug|title|status`). Источник постера: `featured_image`, если пусто — `https://img.youtube.com/vi/<video_id>/maxresdefault.jpg`. Пустой результат показывает плейсхолдер «No articles found…», есть скелетон на время загрузки. Блок вставлен на страницу `school-equipment` между `interactive_header` и `cta_section` (миграция добавлена).
- Новый layout `team_grid` (frontend `TeamGrid`, client) — грид лидеров из `TeamMember` по макету 1920/1189/360: контейнер 1320px, 4× карточки 315×434 (desktop) с radius 10px и тенью `0 2px 20.6px rgba(0,0,0,0.1)`; tablet 4×259.9×358 с radius 8.25px; mobile — карусель 320×441 со стрелками. Типографика: Lorin Bold 22/18.15/20 (name) и Lorin Regular 14/11.55/14 (role), leading 1.2/1.4, цвет brand.dark / brand.dark/70. Отступ от заголовка до сетки: 96px (desktop), 80px (tablet), 109px (mobile); секционный фон по умолчанию `brand.gray`, паддинги `pt-120 pb-120`. Пропсы: `title`, `query{limit, filters{department|slug|is_active}, items[slug...]}`, `padding`, `backgroundClass|backgroundColor`. При заполненном `items` порядок берётся по слугам, иначе `/api/team-members`.
- Layout `team_highlight` (frontend `ContentHighlight`, server) — универсальный highlight‑блок: верхний заголовок + описание по центру, далее карточка с левым изображением 274×343 (radius 20px) и правым текстовым блоком на фоне `brand.gray` (radius 20px, паддинги 48×40, ширина до 1092, текст 24/20), затем опциональный футер‑заголовок + текст **с той же разметкой/типографикой, что и вверху**. Поля layout: `title` (TinyMCE заголовок секции), `description` (TinyMCE описание секции; legacy `intro` поддержан), `image` (+ `alt`) для картинки, `cardTitle` (TinyMCE заголовок карточки), `cardDescription` (TinyMCE текст карточки), `footerTitle` (TinyMCE заголовок футера), `footerText` (TinyMCE текст футера), `padding`, `background*`. Блок больше не зависит от Team API/сущности.
- Новый layout `custom_software` (frontend `CustomSoftware`) — секция из заголовка/описания + подпись и три текстовых карточки на белом фоне, внизу опциональный `footerText` (TinyMCE) с центрированным текстом `font-heading` 16/18/20px `leading-1.4` `text-brand-dark/70`, отступ от сетки 40/48/56px (mobile/tablet/desktop). Макеты 1920/1189/360: ширина контейнера `.container` по Tailwind, контентный блок 974px по центру; заголовок `font-heading` 700 64px/48px/32px (`leading-none`), описание `font-sans` 20px (desktop) / 18px (tablet) / 16px (mobile), `leading-1.4`, цвет `text-brand-dark/70`. Подзаголовок сетки 24px/20px/18px `font-heading` 700, отступы: title→desc 20px desktop, desc→gridTitle 56/64/70px (mobile/tablet/desktop), gridTitle→cards 24/24/30px. Карточки: radius 10px desktop / 9px tablet / 8px mobile, тень `0 2px 20.6px rgba(0,0,0,0.1)` (tablet версия `0 1.65px 16.995px rgba(0,0,0,0.1)`), фон #fff; внутренние отступы 29×32 (desktop), 24×24 (tablet), 20×22 (mobile). Текст карточки `font-heading` 400 18px/17px/16px, `leading-1.4`, цвет `text-brand-dark/70`. Значения приходят из CMS (title/description/gridTitle/items/footerText); если все поля пусты, нет карточек и footerText — блок не рендерится. Блок добавлен на страницу `museum-equipment` после `cta_section` (миграция ниже), при необходимости дефолтный контент задаётся миграцией/данными, а не компонентом.
- Новый layout `software_equipment` (frontend `SoftwareEquipment`) — секция по Figma 1920 с заголовком 64px (`font-heading` bold, `leading-none`) и описанием 20px (`font-sans`, `leading-1.4`, `text-brand-dark/70`) по центру, под ними чип‑лейбл высотой 53px, радиус 100px, фон по умолчанию `#1A1A1A`, текст 16px белый. Ниже грид из 5 круглых иконок 100×100 и подписей 20px (`font-heading` bold), отступы: title→desc 20px, desc→label 40px, label→grid 40px, вертикальный центринг без дополнительного фона. Адаптив: на md заголовок 48px, описание 18px, лейбл 50px высотой, подписи 18px; на base заголовок 32px, описание 16px, лейбл 48px высотой, подписи 16px. Грид ломается 2 столбца на base, 3 на md, 5 на xl; gap 32/40/82px (base/md/xl), горизонтальное центрирование `.container` шириной 1320px (2xl) с `px-5 md:px-8`. Поля блока: `title`, `description`, `label{ text, backgroundColor?, textColor? }`, `items[{ title, icon }]`, `padding`, `backgroundClass`, `backgroundColor`. Добавлен на страницу `sensory-room` **между** блоками `custom_software` и `product_description` (миграция `2025_12_13_150000_add_software_equipment_block_to_sensory_room_page`).
- Новый layout `research_results` (frontend `ResearchResults`) для страницы `special-needs` (после блока `research`): фон `brand.gray`, дефолтные паддинги `pt 82/169/183px`, `pb 140/150/173px` (mobile/tablet/desktop). Заголовок по центру `font-heading` bold `text-brand-dark` 38px→64px (`tracking-[-0.01em]`, `leading-1`), отступ до описания 51px mobile / 15px tablet+. Описание: `font-heading` 16px→20px `leading-1.4` `text-brand-dark/70`, ширина до 320px mobile / 833px tablet+. От описания до сетки 114px mobile / 78px tablet+. Сетка: 1 колонка с `gap-y 10px` на mobile, `3` колонки с `gap 20px` на tablet/desktop; макет рассчитан на 5 карточек (3 в первом ряду, 2 во втором, справа место под декор). Карточка: min-h 160px mobile / 213px tablet+, radius 10px, тень `0 2px 20.6px rgba(0,0,0,0.1)`, паддинги `pt 19/24px`, `pb 20/24px`, `px 16/30px` (mobile/desktop), фон #fff. Титул карточки `font-heading` ExtraBold 24px `leading-1.2` цвет **#67A0FF** (токена нет); текст карточки `font-heading` 16px→20px `leading-1.4` `text-brand-dark/70`, отступ сверху 16px. Декор: svg из макета (`/public/images/research-results/decor-desktop.svg` для tablet/desktop 319×261→387×261, абсолютное позиционирование right/bottom 0) и `/public/images/research-results/decor-mobile.svg` для mobile 319×231, выводится под сеткой. Поля layout: `title`, `description`, `items[{title,text}]`, опционально `decoration`, `decorationMobile`, `padding`, `backgroundClass|backgroundColor`. По умолчанию блок рендерится только при наличии хотя бы заголовка/описания/карточек.
- Новый layout `exclusive_offer` (frontend `ExclusiveOffer`) — секция «Exclusive offer» на фоне `brand.gray`, вставляется на странице `special-needs` между `our_approach` и `news`. Паддинги по умолчанию: mobile `pt 92px / pb 120px`, tablet `pt 140px / pb 150px`, desktop `pt 184px / pb 170px`, переопределяются полем `padding`. Заголовок `font-heading` Bold 32→48→64px (`leading-1`, `tracking-[-0.01em]`, `text-brand-dark`), ширина 320→974px; отступ до описания 12px mobile / 15px tablet+. Описание (RichText) Lorin Regular 16→20px `leading-1.4`, `text-brand-dark/70`, ширина 320→894px. Отступ от описания до карточек: 54px mobile, 92px tablet, 102px desktop. Сетка: 1 колонка на mobile, 2 колонки с `gap-x 20px`/`gap-y 20px` c md. Карточки: фон `brand.gray`, radius 10px, min-h 232px mobile / 262px desktop, паддинги `pt 24/27px`, `pb 24/28px`, `px 22/30px` (mobile/desktop). Заголовок карточки `font-heading` ExtraBold 20→22px `leading-1.4` `text-brand-dark`; текст (RichText) 18→20px `leading-1.4` `text-brand-dark/70`, `strong` рендерится ExtraBold `text-brand-dark`, отступ сверху 16/19px. CTA: кнопка 158×53, radius 100px, фон `brand.sky` (#00AAE8), текст white 16px Bold, hover чуть темнее; отступ текст→CTA 32px mobile / 46px desktop. Поля: `title`, `description` (RichText), `items[{ title, text (RichText), ctaLabel?, ctaHref? }]`, `padding`, `backgroundClass|backgroundColor`; при отсутствии контента блок не рендерится. Отдельных макетов для tablet/mobile в Figma нет — размеры взяты пропорционально desktop макету и зафиксированы здесь.

- Новый layout `summer_camp` (frontend `SummerCamp`) — блок по макету «Summer Camp» на белом фоне. Паддинги по умолчанию: mobile `pt 96px / pb 120px`, tablet `pt 120px / pb 140px`, desktop `pt 150px / pb 150px`, задаются `padding`. Контейнер `.container`, контент шириной до `1320px`. Заголовок `font-heading` Bold 32→48→64px, `leading-1`, `tracking-[-0.01em]`, цвет `brand.dark`. Характеристики (feature list) под заголовком: вертикальный `gap 10px`, иконка 24×24 (из поля `icon`, иначе фолбек `Calendar`/`MapPin` `brand.sky`), текст `font-heading` 16→20px `leading-1.4`; `label` обычный вес, `value` ExtraBold. Отступ от заголовка до характеристик 40px, от характеристик до описания 26px (если нет характеристик — 22px). Описание (RichText) `font-heading` 16→20px `leading-1.4` `text-brand-dark/70`, ширина до 320/720/833px. Видео: превью берётся автоматически из YouTube по `videoId` (`maxresdefault.jpg`), радиус 12/14/17px, аспект `2.18`, `min-h 240/360/604px`, отступ сверху 78px. Оверлей: позиция mobile — центр снизу (`bottom 94px`, `left 50%`), tablet `left 28px bottom 32px`, desktop `left 44px bottom 94px`; кнопка Play 149×50 `rounded-full` белая с тенью `0 10px 30px rgba(0,0,0,0.12)`, текст `font-heading` 16px ExtraBold `text-brand-dark`, внутри круг 35px `brand.dark` с белым Play; рядом `Learn More` 16px Bold `brand.dark`, опциональная ссылка `learnMoreHref`. Клик по постеру или Play открывает модал с YouTube `nocookie` iframe, блокировка скролла, закрытие крестиком или кликом по фону. Поля: `title`, `description` (RichText), `features[{label, value, icon}]`, `videoId`, `learnMoreHref`, `padding`, `backgroundClass|backgroundColor`. Блок рендерится при наличии любого из: заголовок/описание/характеристики/видео.

- Новый layout `game_distribution` (frontend `GameDistribution`) — двухколоночный блок «Free Game Distribution: A Commitment to Education» с фоном `bg-brand-gray`, top/bottom padding 98px mobile / 126px tablet+desktop (override через `padding`), фон можно переопределить `backgroundClass`/`backgroundColor`. Mobile (360px): стэк, сначала медиа 320×227px radius 17.23px, затем заголовок `font-heading` 700 38px `leading-none` + `tracking-[-0.01em]`, отступ медиа→title 35px, title→description 24px, description `font-sans` 16px `leading-1.4` `text-brand-dark/70`, контейнер `.container` с max-w 320px и padding 20px. Tablet (~1189): `.container`, внутренний блок max-w 1089px, flex-row с gap 93px, текстовая колонка max-w 568px (title 64px, description 20px), медиа 428×486px radius 17.23px. Desktop (1920): `.container`, inner max-w 1320px, gap 93px, текстовая колонка max-w 568px, медиа 659×486px radius 17.23px. При наличии `videoId` поверх постера показывается play-иконка (64px круглой кнопкой), клик открывает modal с YouTube (`nocookie`, autoplay, playsinline). Постер берётся из `image` (relative/abs path) или из превью YouTube по `videoId`, alt — поле `alt`. При отсутствии title+description+poster блок не рендерится. Макет добавлен на страницу `museum-equipment` сразу после `custom_software` (миграция `2025_12_11_232000_add_game_distribution_block_to_museum_page.php`), дефолтные значения: текст из макета, `image=/images/free-game-distribution.jpg`, `alt="Children exploring an interactive sandbox exhibit"`, `videoId=null`.
- Новый layout `practice_showcase` (frontend `PracticeShowcase`) — двухколоночный блок «How Does It Work in Practice?» с фоном `bg-brand-gray`, default padding top/bottom 80px (90px md+). Desktop макет 1920: `.container` max-w 1320px, flex row center aligned, gap 60px, текстовая колонка max-w 663px, медиа 598×326px radius 10px с aspect `[299/163]`. Типографика: title `font-heading` 700 24px `leading-1.2` `text-brand-dark`; описание `font-sans` 20px `leading-1.4` `text-brand-dark/70`, margin title→text 20px, абзацы без внешних отступов. Play кнопка при `videoId`: круглая 48px, фон black/65, иконка Play 20px; открывает modal YouTube (`nocookie`, autoplay, playsinline). Poster берётся из `image` (или превью YouTube при отсутствии), alt — поле `alt`. Блок не рендерится, если нет title, description и постера. Миграция `2025_12_11_233000_add_practice_showcase_block_to_museum_page.php` добавляет блок на `museum-equipment` сразу после `game_distribution` с дефолтами: заголовок/описание по макету, `image=/images/work-in-practice.jpg`, `alt="Child smiling during interactive projection experience"`, `videoId=null`.
- Для блоков с API‑фильтрами добавлен приоритетный выбор конкретных элементов: `news.query.items[slug[]]`, `product_carousel.query.items[slug[]]`, `games_gallery.query.items[slug[]]`, `games_grid.query.items[slug[]]`. Если `items` указаны, блок рендерит только эти записи в заданном порядке, игнорируя limit/filters; в админке — multiselect из соответствующей сущности. Пустые или строковые значения (`""` или `slug1,slug2`) автоматически нормализуются в список.
- Новый layout `hospital_equipment` (frontend `HospitalEquipment`) — трёхсекционный блок под макеты 1920/1189/360: верхний градиент `linear-gradient(90deg,#FAE2FF→#ACD3FF)` с V‑разделителями (53/120/78px top/bottom, CSS clip-path), заголовок 64px (38px mobile) с опциональным описанием 18–20px под ним (`description`, RichText), адаптивная сетка фич без фиксированного числа колонок (auto-fit `minmax≈240px,1fr` на десктопе; gap 36px/29px; мобильный стек с gap 44px). Секция целиком скрывается, если одновременно пусты `title`, `description` и все фичи без контента. Каждая фича: белый круг 103px, иконка 65px (PNG/SVG из `pages/hospital_equipment/features`, дефолты в `frontend/public/images/hospital-equipment/feature-[1-4].png`), заголовок 16px 800 `leading-1.4`, описание 16px `rgba(26,26,26,0.7)` рендерится на фронте через `RichText`. CTA‑секция на тёмном фоне с бэкграундом `/images/hospital-equipment/cta-bg.jpg` + overlay 0.7, заголовки 64/38px (градиентная вторая строка), **опциональное описание `ctaDescription` между заголовком и кнопкой** (также `RichText`), кнопка 248×53 radius 100px. Нижний блок: иконка 36px (default `/images/hospital-equipment/rocket.svg`) + заголовок 24px 800, абзац 16px `rgba(26,26,26,0.7)` (`RichText`); весь футер скрывается, если `footerTitle` не заполнен. Поля layout: `title`, `description`, `features[{title,description,icon}]`, `ctaTitle`, `ctaGradient`, `ctaDescription`, `ctaLabel`, `ctaHref`, `ctaBackground`, `footerTitle`, `footerDescription`, `footerIcon`, `padding`. В админке описания (description/features.description/ctaDescription/footerDescription) вводятся через TinyMCE `->unescape()` без экранирования спецсимволов. Миграция `2025_12_07_000300_seed_hospital_equipment_block` добавляет блок с дефолтными ассетами к странице `slug=hospital-equipment`.
- Новый layout `special_needs` (frontend `SpecialNeeds`) — видео‑грид по макетам 1920/1189/360 на фоне `brand.gray`. Паддинги по Figma: мобайл 65/83px (top/bottom), планшет 88/165px, десктоп 184/258px; отступы title→desc 12/15px, desc→grid 35/38px. Заголовок: `font-heading` 700, 38px (mobile) / 64px (md+), `leading-1`, цвет #1A1A1A. Описание: `font-sans`/Lorin Regular 16px (mobile) / 20px (md+), `leading-1.4`, `text-brand-dark/70`, ширина 320px / 934px. Сетка: `grid-cols-1 md:grid-cols-2`, ширина 320px / 1089px / 1320px, горизонтальный gap 42px, вертикальный gap 12px; карточки — скругление 10px, высота 163px (mobile) / 326px (md+). Плей‑кнопка 64px (как в InteractiveShowcase) с тенью, постер берётся автоматически с `https://img.youtube.com/vi/<videoId>/maxresdefault.jpg`. Клик открывает модалку `youtube-nocookie` (autoplay), body scroll блокируется. Поля layout: `title`, `description` (RichText), `videos[{ videoId, alt? }]`, `padding`, `backgroundColor`. Миграции: `2025_12_09_130100_add_special_needs_block_to_hospital_page` добавляет блок после `discount_banner` на странице `slug=hospital-equipment`; `2025_12_09_130200_cleanup_special_needs_block` убирает устаревшие поля `image`/`videoUrl`.
- Layout `interactive_header` теперь рендерится компонентом `InteractiveShowcase` (две карточки: текст + фото/видео с чередованием колонок, плитка фич, CTA + галерея/видео-модалки). Пропсы: `items[{ title, productPageSlug?, description (rich text), formDescription? (rich text для описания формы под заголовком в QuoteModal), hashtag, features[{ icons?[], label }], ctaLabel, formCode?, gallery[{src,alt}], videoId? }]`, `padding`, `defaultFormCode`, `formConfig`. В админке каждая feature имеет фиксированные поля `icon1|icon2|icon3` (Image) + `label` — никаких `Image::multiple()` внутри Json (MoonShine падает из-за `ComponentSlot::__toString()`), поэтому бэкенд при отдаче API собирает `icons[]` из непустых `icon1..3` и добавляет туда легаси `icons[]`, если они были. FeatureCard рисует все элементы массива `icons` без лимита. Видео-превью берётся из YouTube (`https://img.youtube.com/vi/<id>/maxresdefault.jpg`), без ручных постеров/alt. CTA ведёт на `productPageSlug` (если выбран) **только когда** нет формы; при наличии `formCode` (или `defaultFormCode`) CTA открывает QuoteModal (приоритет формы); если нет ни формы, ни productPageSlug — CTA скрывается. Если задан `productPageSlug`, заголовок ведёт на соответствующую product_landing страницу (`/${slug}`). Автоподстановка пресетов не используется; данные приходят из админки MoonShine.
- Layout `interactive_header` теперь рендерится компонентом `InteractiveShowcase` (две карточки: текст + фото/видео с чередованием колонок, плитка фич, CTA + галерея/видео-модалки) с общим заголовком `title` над списком элементов. Пропсы: `title`, `items[{ title, productPageSlug?, description (rich text), formDescription? (rich text для описания формы под заголовком в QuoteModal), hashtag, features[{ icons?[], label }], ctaLabel, formCode?, gallery[{src,alt}], videoId? }]`, `padding`, `defaultFormCode`, `formConfig`. В админке каждая feature имеет фиксированные поля `icon1|icon2|icon3` (Image) + `label` — никаких `Image::multiple()` внутри Json (MoonShine падает из-за `ComponentSlot::__toString()`), поэтому бэкенд при отдаче API собирает `icons[]` из непустых `icon1..3` и добавляет туда легаси `icons[]`, если они были. FeatureCard рисует все элементы массива `icons` без лимита. Видео-превью берётся из YouTube (`https://img.youtube.com/vi/<id>/maxresdefault.jpg`), без ручных постеров/alt. CTA ведёт на `productPageSlug` (если выбран) **только когда** нет формы; при наличии `formCode` (или `defaultFormCode`) CTA открывает QuoteModal (приоритет формы); если нет ни формы, ни productPageSlug — CTA скрывается. Если задан `productPageSlug`, заголовок ведёт на соответствующую product_landing страницу (`/${slug}`). Автоподстановка пресетов не используется; данные приходят из админки MoonShine.
- Layout `interactive_equipment` рендерится компонентом `InteractiveEquipment` (серия секций «Interactive Sandbox/Floor/…»): общий `title`/`description` над списком; `items[{ title, description? (rich text), body? (rich text), tabs[{label, icon?, content? (rich text)}], videoId?, videoCaption? (rich text), primaryCtaLabel, primaryCtaFormCode?, primaryCtaFormTitle?, secondaryCtaLabel, secondaryCtaFormCode?, secondaryCtaFormTitle?, specialistsTitle?, specialistsLeft[{text}], specialistsRight[{text}], reviewsTitle?, reviews[{name, meta?, text?, videoId?}] }]`, `padding`, `backgroundColor`. Upload dirs: tabs icons → `public/storage/pages/interactive_equipment/tabs`. CTA-кнопки открывают QuoteModal по выбранному `Form.code` (если formCode пустой — кнопка скрывается).
- Динамическое SEO: `generateMetadata` на маркетинговых, новостных, игровых и store‑страницах читает SEO-поля из API-ответов.

## Бэкенд Laravel (директория `backend/`)
- Вход: `public/index.php`; API и админка обслуживаются через Nginx.
- Основные маршруты (`routes/api.php`):
  - `GET /api/health` → `{ ok: true }`
  - `GET /api/menus?location=header|footer` — активные меню с вложенными пунктами (`label`, `url`, `slot`, `icon`, `opens_in_new_tab`, `children`).
  - `GET /api/site-settings` — singleton-настройки сайта (`logo_url`, `header_phone`, `header_whatsapp`, `contact_*`, `support_*`, `social_links[{label,href,icon,targetBlank,color,headerColor,footerColor}]`); используется шапкой и футером как единственный источник логотипа, контактов и соцсетей. Если запись ещё не создана, эндпоинт возвращает `404` без каких-либо дефолтов — сначала заведите данные через MoonShine. `icon` поддерживает Iconify ID (`set:name`, например `mdi:facebook` или `streamline:facebook`); `color` сохраняется для совместимости и применяется по умолчанию, `headerColor`/`footerColor` позволяют задать отдельные HEX-значения (`#RRGGBB`) для шапки и футера.
  - Контентные страницы: `GET /api/pages/{slug}` — только `status=published`, ответ `PageResource` (`slug`, `title`, `type`, `seo{title,description,canonical,og_image}`, `blocks[{name,key,values}]`); API не перекладывает их в `layout/fields`, фронт сам мапит `name→layout`, `values→fields` при рендере.
  - Статьи: `GET /api/articles?type&category&limit&page`, `GET /api/articles/{slug}` — только опубликованные; `ArticleResource` включает SEO и категории.
  - Игры: `GET /api/games?limit`, `GET /api/games/{slug}` (`GameResource`, категории, products_used, game_type, video_id, video_url, is_indexable).
  - Продукты-лендинги: `GET /api/products?limit`, `GET /api/products/{slug}` (`ProductResource` с variants и связанной form для CTA).
  - Sensory Room bundles: `GET /api/sensory-room-bundles?limit&filter[slug]&filter[title]`, `GET /api/sensory-room-bundles/{slug}` — только `status=published`, ответ `SensoryRoomBundleResource` (`slug`, `title`, `excerpt`, `gallery[{src,alt}]`, `specs[string[]]`, `form_code`, `custom_bundle_url`, `products[{slug,name}]`, детальные блоки `block_a_*`/`block_b_*`, SEO). Для детального эндпоинта дополнительно добавляются `breadcrumbs[{label,href}]` (лейблы берём из Pages `home` и `sensory-room`, чтобы не дублировать строки во фронтенде).
  - Магазин: `GET /api/store/products?limit&available`, `GET /api/store/products/{slug}` (`StoreProductResource` с категориями, доступностью, specs).
  - Отзывы: `GET /api/reviews?limit&fields&filter[ids]=1,2&filter[is_active]=true` — по умолчанию только активные, сортировка `position` → `review_date` → `id`; отдаёт name/date/rating/text/avatar/source_url/position/is_active.
  - Формы: `GET /api/forms/{code}` — возвращает `code`, `title`, `topic`, `submit_label`, `success_message`, `fields[]` (name/label/type/options/required); `POST /api/forms/{code}` — принимает `topic` (из запроса или из формы), валидирует обязательные поля (email + required из `forms.config`), создаёт `Lead` с `topic`, `payload`, `source_url`, `utm`, `submitted_at` (timestamp сервера), ответ `{ success: true }` 201.
- Веб-роут `/` (стандартный `welcome`), остальное закрыто MoonShine.

### Модели и таблицы (кастомные)
| Модель | Таблица | Ключевые поля/касты | Связи |
|---|---|---|---|
| `Page` | `pages` | `slug` unique, `title`, `type` (`product_landing` \| `static`), `product_id` nullable FK → `products`, `status` (draft/published), `blocks` jsonb (LayoutsCast; хранит элементы как `{name,key,values}`), SEO-поля, `published_at` (автозаполнение текущим временем при переводе в `published`, если пусто); accessor `blocks_array`; slug `home` зарезервирован под главную страницу и создаётся миграцией | `product` belongsTo |
| `Article` | `articles` | `slug` unique, `title`, `type`, `excerpt`, `body`, `featured_image`, `video_id`, `status`, SEO, `published_at` (cast datetime, автозаполняется текущим временем при переводе в `published`, если поле пустое) | `categories` many-to-many `ArticleCategory` |
| `ArticleCategory` | `article_categories` | `slug` unique, `name`, `group`, `parent_id` self-FK, `position` int (order within родителя) | `articles` m2m, `parent`/`children` (ordered by `position`) |
| `Game` | `games` | `slug` unique, `title`, `genre`, `target_age`, `game_type`, `excerpt`, `body`, `hero_image`, `video_id` (YouTube), `video_url`, `is_indexable` bool, SEO | `categories` m2m `GameCategory`, `products` m2m |
| `GameCategory` | `game_categories` | `slug` unique, `name`, `description` | `games` m2m |
| `Product` | `products` | `slug` unique, `name`, `slogan`, `excerpt`, `description`, `hero_image`, `default_cta_label`, `rating` decimal(2,1), `review_count_label`, `badges` jsonb (cast array), `form_id` FK → `forms` | `variants` hasMany (ordered by `position`), `games` m2m, `form` belongsTo, `sensoryRoomBundles` m2m |
| `SensoryRoomBundle` | `sensory_room_bundles` | `slug` unique, `title`, `excerpt`, `gallery` jsonb (cast array, элементы `{src,alt}`), `specs` jsonb (cast array, элементы `{value}`; публичный API отдаёт `specs` как `string[]`), `form_code` nullable (ссылка на `forms.code` для QuoteModal), `custom_bundle_url` nullable (произвольная ссылка для кнопки “Custom Bundle”), детальные блоки `block_a_title`, `block_a_items` jsonb (cast array, `{icon,text}`; legacy title объединяется в text миграцией), `block_b_title`, `block_b_text`, `status` (`draft`/`published`), `position` int, SEO | `products` m2m (`product_sensory_room_bundle`) |
| `ProductAttribute` | `product_attributes` | `name`, `code` nullable, `type` (`string`/`number`/`boolean`/`json`), `position` int | `values` hasMany `ProductVariantAttributeValue`; m2m `variants` через `product_variant_attribute_values` |
| `ProductVariant` | `product_variants` | FK `product_id`, `name`, `image` (upload), `price` decimal(12,2), `label`, `position`, `is_highlighted` boolean default `false` (CompareModels highlight flag) | `product` belongsTo; hasMany `attributeValues` (with `attribute`), typed shortcuts `attributeValuesString/Number/Boolean/Json`; accessor `specs` собирает key→typedValue из `attributeValues` (key = `attribute.code` или slug `name`); `spec_label_map` хранят `attribute.name` для CompareModels; hasMany `ProductVariantSpec` (legacy specs) |
| `ProductVariantAttributeValue` | `product_variant_attribute_values` | FK `product_variant_id`, `product_attribute_id`, `attribute_type`, `value` jsonb (casts), `position`; unique (`product_variant_id`,`product_attribute_id`) | `variant` belongsTo, `attribute` belongsTo; accessors `value_string/number/boolean/json` и `typedValue()` |
| `ProductVariantSpec` | `product_variant_specs` | FK `product_variant_id`, `key`, `value` text, `type` (`string`/`number`/`boolean`/`json` как строка), `position` | `variant` belongsTo |
| `ProductSpecTab` | `product_spec_tabs` | FK `page_id`, `block_key`, `block_index`, `key`, `label`, `image`, `title`, `description`, `position` | `page` belongsTo; привязка к конкретному блоку specs на странице |
| `StoreProduct` | `store_products` | `slug` unique, `name`, `excerpt`, `description`, `image`, `price`, `is_available` bool, `specs` jsonb, SEO | `categories` m2m `StoreCategory` |
| `StoreCategory` | `store_categories` | `slug` unique, `name`, `parent_id` self-FK, `position` int | `products` m2m, `parent`/`children` (ordered by `position`) |
| `Form` | `forms` | `code` unique, `title`, `topic`, `config` jsonb (cast array) | `leads` hasMany via `form_code` |
| `Lead` | `leads` | `form_code`, `topic`, `product_variant_id` FK → `product_variants`, `payload` jsonb, `source_url`, `utm` jsonb (casts array), `submitted_at` datetime | `productVariant` belongsTo |
| `SiteSettings` | `site_settings` | singleton с полями `logo` (путь к логотипу), `header_phone`, `header_whatsapp`, `contact_address_line1/2`, `contact_phone_main`, `contact_phone_main_label`, `contact_phone_whatsapp`, `contact_phone_whatsapp_label`, `contact_email`, `contact_hours`, `support_phone`, `support_phone_label`, `support_email`, `social_links` jsonb (cast array, элементы `{ label, href, icon, targetBlank, color, header_color, footer_color }`) | —; используется API `/api/site-settings` и MoonShine ресурсом для редактирования |
| `Menu` | `menus` | `name`, `slug` unique, `location` (`header`/`footer`), `is_active` bool | `items` hasMany (`MenuItem`) |
| `MenuItem` | `menu_items` | `menu_id` FK, `parent_id` self-FK, `label`, `url` nullable (when absent item is rendered as plain text), `slot` (primary/top_primary/top_secondary/footer), `icon` nullable, `opens_in_new_tab` bool, `is_active` bool, `position` int | `menu` belongsTo; self `parent`/`children` |
| `Review` | `reviews` | `name`, `review_date` cast `date`, `rating` tinyint 1–5, `text`, `avatar`, `source_url`, `position` int, `is_active` bool (default true) | — |
| `TeamMember` | `team_members` | `name`, `slug` unique, `role`, `department`, `photo`, `bio`, `is_active` bool default true, `position` int | — |
| `User` | `users` | стандартный Laravel, `password` hashed | — |

### Pivot-таблицы
- `article_article_category` (article_id, article_category_id, PK составной, cascade).
- `game_game_category` (game_id, game_category_id).
- `store_category_store_product` (store_product_id, store_category_id).
- `product_sensory_room_bundle` (product_id, sensory_room_bundle_id, unique, cascade).

### MoonShine (админка)
- Провайдер `App\Providers\MoonShineServiceProvider` регистрирует все ресурсы; layout `App\MoonShine\Layouts\MoonShineLayout` (палитра Purple, кастомное меню). В `App\Providers\AppServiceProvider` вручную регистрируем `MoonShine\TinyMce\Providers\TinyMceServiceProvider`, т.к. auto-discovery его не подтягивает и без него падают TinyMCE-поля в админке.
- Адрес админки: `/admin` (prefix из `config/moonshine.php`), аутентификация `moonshine` guard.
  - Ресурсы (CRUD):
  - **Контент:** `PageResource` (конструктор блоков: hero, features_grid, games_list, news_list, quote_form), `ArticleResource` (типы: news/case_study/blog/in_press, категории m2m), `ArticleCategoryResource` (иерархия категорий), `SensoryRoomBundleResource` (галерея/спеки jsonb, связь с Products, `form_code` для QuoteModal; на Detail-странице JSON поля `gallery/specs/block_a_items` отображаются табличным превью с картинками/текстом).
  - **Игры:** `GameResource` (genre/target_age, hero_image, m2m категории), `GameCategoryResource`.
  - **Продукты:** `ProductResource` (rating/review_count_label, badges json, form belongsTo Form; без SEO-полей) и `ProductVariantResource` (image upload `products/variants`, price/label/position). Атрибуты варианта редактируются четырьмя `RelationRepeater` (кастом `RelationRepeaterWithMutators`, чтобы апдейты гонялись через модельные мутаторы): отдельно для `string/number/boolean/json`, каждый жестко фильтрует `ProductAttribute` по типу (в `valuesQuery`) и пишет FK в колонку `product_attribute_id`; колонка Type выводится как read-only текст с дефолтом соответствующего блока, сразу видна при добавлении и уходит в форму. Значение хранится в колонке `value` (jsonb), но для json-типа форма теперь читает/пишет через аксессор `value_json` (список `{key,value}`); мутаторы кодируют нормализованный массив в JSON-строку перед сохранением, чтобы каст `json` не пытался декодировать уже готовый массив. Для сохранения используется свежий relation‑builder на каждую строку (после очистки лишних записей), чтобы не оставались `whereNotIn` из шага удаления и апдейты существующих значений проходили через мутаторы/касты.
    - string → сохраняется как JSON-строка (json_encode),
    - number → число,
    - boolean → bool,
    - json → объект, собранный из key/value.
    Тип определяется по `attribute_type`. Серверная валидация `exists ... where type=?` не даёт выбрать чужой тип. `ProductAttributeResource` управляет справочником атрибутов, `ProductVariantAttributeValueResource` — значениями; превью `image` выводится с диска `public/products/variants`.
  - Все поля `Slug` в MoonShine ресурсах генерируются реактивно от `title`/`name` с локалью `ru` (транслитерация кириллицы) без задержки, но при наличии собственного значения slug не перезаписывается.
  - В `Page`/`Article` формах `Published at` по умолчанию проставляется текущим временем; при сохранении записи в статусе `published` пустое поле также автозаполняется на уровне модели.
  - В списке Pages добавлен MoonShine-фильтр Select по `type` (`product_landing`/`static`), чтобы быстро разделять лендинги продуктов и статические страницы.
  - В списке Pages в выпадающем меню строк добавлен Clone: модалка с полями Title/Slug (предзаполнены `<title> (copy)` и `<slug>-copy` с авто-уникализацией). Копирует всю страницу вместе с блоками/SEO/связями и создаёт новый `Page` со статусом `draft` и пустым `published_at`, после сохранения редиректит на форму редактирования.
  - **Магазин:** `StoreProductResource` (availability switcher, price, categories m2m), `StoreCategoryResource` (self-parent).
  - **Формы и лиды:** `FormResource` (topic, JSON-конфиг форм: submit_label, success_message, поля с типами text/email/phone/textarea/select/checkbox), `LeadResource` (topic, payload, utm JSON; деталь выводит payload/utm таблицей key→value плюс source_url, submitted_at и связанный product_variant; в списке лидов включён мультивыбор с массовым удалением как в остальных ресурсах).
- **Отзывы:** `ReviewResource` (name, review_date, rating 1–5, текст TinyMCE, аватар upload в `storage/app/public/reviews` с валидацией `image|max:5MB`, source_url, position, active).
- **Навигация:** `MenuResource` (создание/активация меню с локацией header/footer) и `MenuItemResource` (пункты со слотами, иконками, таргетами, вложенностью и позицией; URL опционален — пустое значение делает пункт не кликабельным). Индекс Menu Items — дерево с drag-and-drop сортировкой (MoonShine Tree Resource) без автопринудительной фильтрации: по умолчанию показываются все меню, а через OffCanvas-фильтр можно явно сузить список по menu/slot. В форме Parent выбирается из элементов текущего меню. Штатные фильтры MoonShine не работают с TreeComponent, поэтому на странице дерева есть собственный OffCanvas-фильтр (кнопка Filters в топ-баре) с полями menu/slot/parent; `MenuItemResource::modifyQueryBuilder` строит новый запрос без скрытых `menu_id` дефолтов и вручную применяет только явные `filters[menu|slot|parent]`.
- **Настройки:** `SiteSettingsResource` — ресурс для глобальных настроек сайта (логотип, телефоны и email’ы контактов/поддержки, соцсети). По договорённости используется одна запись (singleton): фронт/шапка/футер всегда берут первую строку из `site_settings`. Для соцсетей можно задать Iconify ID и отдельные HEX-цвета для шапки и футера (`header_color`, `footer_color`), либо оставить пусто, чтобы применялся общий цвет/градиент; поля цветов редактируются через встроенный color picker MoonShine (`Color` field).
- **Системные:** `MoonShineUserResource`, `MoonShineUserRoleResource` (стандартные ресурсы пакета).
- **Деревья с drag-and-drop:** `ArticleCategoryResource`, `StoreCategoryResource` и `MenuItemResource` переведены на `lee-to/moonshine-tree-resource` (MoonShine v4). Индекс-страницы рисуют дерево с сортировкой перетаскиванием и автоматическим сохранением `position`; массовое удаление отключено. Для MenuItem дерево можно фильтровать по меню/слоту вручную, но по умолчанию отображаются все меню; новые узлы получают позицию в конце ветки, поле `Position` из формы меню убрано.
- В `PageFormPage` у конструктора блоков включён кастомный `onApply` для `Layouts`, чтобы MoonShine сохраняла dot-ключи вложенных полей (`padding`, `query.*`) из `blocks[...]` как есть; ранее они затирались пустыми строками при сохранении. Поле `padding` теперь одно строковое (класс/стили Tailwind); пустое значение оставляет дефолтные отступы блока. Аккордеоны Layouts рендерятся через кастомный оверрайд `moonshine-layouts-field/layout` с `persist=false`/`open=false`, поэтому блоки всегда загружаются свёрнутыми и не запоминают открытое состояние (ручное раскрытие остаётся штатным, валидация продолжает раскрывать нужный блок через `collapse-open`).
- Все CRUD-страницы используют валидации уникальности slug/code и базовые required-правила; загрузки файлов ведутся на диск `public` в подкаталоги `seo/`, `pages/hero`, `articles`, `games`, `products`, `store`.

## Инфраструктура
- **Оркестрация:** `docker/compose.sh <environment> …` (обязательное использование). Композ-файлы: `docker/development.compose.yml`, `docker/production.compose.yml`. Env-файлы: `.env.development`, `.env.production`.
- **Сборка backend-php:** в контексте `backend/.dockerignore` исключает `vendor/`, `storage/`, `bootstrap/cache`, `.env`, `node_modules`, чтобы образ всегда собирал зависимости через `composer install --no-dev` внутри Dockerfile и не подтягивал локальный vendor.
- **Сервисы (dev):**
  - `frontend`: Next dev server на 3000, код смонтирован.
  - `backend-php`: PHP-FPM 8.4, Laravel код смонтирован; общий volume `storage` монтируется в `/var/www/backend/storage` для пользовательских файлов. Лимит памяти PHP в dev поднят до `512M` через `php.dev.ini` → `/usr/local/etc/php/conf.d/php-memory.ini`; лимит количества одновременных файловых полей (`max_file_uploads`) увеличен до `150`, чтобы MoonShine формы с большим числом иконок/галерей сохранялись без ошибки "Maximum number of allowable file uploads has been exceeded".
  - `nginx`: слушает :8080, подхватывает `nginx/nginx.dev.conf`, маунты `backend/public` и общий volume `storage` (RO) для раздачи `/storage`.
  - `postgres`: `postgres:18-alpine`, volume `postgres-data`.
- **Сервисы (prod):** сборка prod-образов Next/Laravel/Nginx, порт :80; `APP_DEBUG=false`.
  - `backend-php` (PHP-FPM 8.4) и `nginx` разделяют volume `storage`, чтобы загруженные через Laravel файлы (`storage/app/public`) сразу раздавались по `/storage/*`. Лимит памяти PHP в prod выставлен на `256M` через `php.prod.ini` → `/usr/local/etc/php/conf.d/php-memory.ini`; параметр `max_file_uploads` увеличен до `150` по той же причине, что и в dev (массовые аплоады в MoonShine).
  - В `nginx.prod.conf` `/storage/` настроен через `alias` на `storage/app/public` (без проксирования в PHP) с CORS `Access-Control-Allow-Origin: *`.
- **Nginx:** `nginx/nginx.*.conf` — роутинг `/admin` и `/api` в php-fpm, статические Laravel ассеты (`/build`,`/storage`,`/vendor`) обслуживаются напрямую; `/storage/*` отдаётся напрямую из volume `storage/app/public` (без `try_files` и php-fpm, с CORS `*` и кешом 7d), остальное проксируется в Next (в prod кэширование выключено через `proxy_cache_bypass`). Блокировка индексации убрана: X-Robots-Tag не выставляется, `/robots.txt` и `sitemap*.xml` раздаются как статические файлы, сгенерированные `next-sitemap`.
- **База данных:** host `postgres`, port `5432`; миграции покрывают все сущности, дополнительные изменения в схеме допускаются только через миграции.
 - **База данных:** host `postgres`, port `5432`; миграции покрывают все сущности, дополнительные изменения в схеме допускаются только через миграции. В проде в `.env.production` обязательно прописаны `DB_CONNECTION=pgsql` (и `DB_*` реквизиты) + `SESSION_CONNECTION=pgsql`, чтобы Laravel не откатывался на sqlite по умолчанию.

## Взаимодействие данных
- Публичные страницы, новости, кейсы, игры и каталог читаются с Laravel API (pages/articles/games/products/store_products) через Next.js `fetch`/`renderBlocks`; хедер/футер подтягивают меню `header`/`footer` с `/api/menus` (fallback на статические пресеты при пустом ответе).
- API нормализует медиа-пути через `Storage::disk('public')->url`, поэтому `hero_image`/`featured_image`/`og_image`/`image` уже приходят как `/storage/...` или абсолютный URL и могут использоваться фронтом напрямую. Все публичные эндпоинты поддерживают опциональные `filter[field]=value` (белый список на уровне контроллеров) и `fields=slug,name,...` для выборки ограниченного набора полей без изменения базового функционала.
- Слайдер «Trusted by»: сущность `TrustedLogo` (таблица `trusted_logos`, поля image, alt, is_active, position), управление в админке через MoonShine (ресурс TrustedLogoResource, загрузка в `storage/app/public/trusted-logos`), публичный API `GET /api/trusted-logos` возвращает активные логотипы по position.
- Отзывы для блока `Reviews`: сущность `Review` (CRUD в админке, меню `Reviews`), публичный API `GET /api/reviews` (по умолчанию активные, сортировка position → review_date → id, фильтры ids/rating/is_active, fields). Блок на фронте всегда сам загружает данные из API (опциональный `query{limit,fields,ids,onlyActive}`), внешние `items` не принимаются; поле Items в конструкторе удалено, наполнение только через API. Поддерживается параметр `template` (`featured` | `compact`): `featured` — только крупный слайдер 2×434px (≥1024px) / 1×468px (mobile) с точками; `compact` — блок средней оценки + количества отзывов и компактный слайдер (3 колонки ≥1280, 2 ≥1024, 1 на mobile). Цвета/градиенты: фон `brand.gray`, текст `brand.dark`, звёзды `ui.star`, CTA использует `bg-brand-gradient animate-gradient` как в кнопке Live demo. Радиус карточек 10px, тень `0 2px 20.6px rgba(0,0,0,0.1)`, текст в карточках ограничен по высоте, при переполнении доступен скролл без затемнения.
- Команда: `TeamMember` (таблица `team_members`: name, slug unique, role, department, photo, bio, is_active bool, position int). Публичный API `GET /api/team-members` (фильтры slug/name/role/department/is_active, fields, limit<=100, сортировка position→id) и `GET /api/team-members/{slug}`. MoonShine ресурс `TeamMemberResource` (фото на `public/team-members`, bio на TinyMCE, position для сортировки, slug автогенерация). Сидер `2025_12_11_000700_seed_team_members` кладёт 15 лидеров для демо.
  - Структурированные контент-блоки страниц (`pages.blocks`) сохраняются в формате MoonShine Layouts (`{ name, key, values }`, каст LayoutsCast). Актуальные layout'ы в админке: hero (title, slides[{videoId?, image?, alt?}] — можно либо загрузить изображение на диск `public/pages/hero` для статичного кадра, либо указать YouTube videoId, тогда фронт рендерит превью и кнопку Play с модалкой), **page_header** (variant: `title` (градиентный H1) или `centered_image` с загруженной картинкой; поля title (для H1 или `sr-only` подписи), image/imageAlt, фон `bg-brand-gray`, опциональный `padding` строкой классов), **hero_values** (title/subtitle/text/cta + items{title,description,icon}; `icon` загружается как SVG-файл в `public/storage/pages/hero_values/icons` и на фронте рисуется `<img>`; columns настраивается, цвет/вариант фиксированы как в дизайн-референсе — sky/values), **product_hero** (title/slogan/description/rating/reviewCount/badgeVariant=image|card/badges[{image,label}]/ctaLabel/formCode/formTitle; `formCode` выбирается из сущности Forms в админке и при наличии перекрывает product.form.code; опция `useProductData=true|false`, CTA показывается всегда при наличии `formCode` (для статичных страниц без продукта тоже), но если `useProductData=true` и продукт не пришёл, CTA не рендерится; при указании `formCode` форма подтягивается на сервере), product_description (title/description), product_nav (items[{label,anchor}] — навигация по якорям секций, anchor задаётся без `#`, компонент сам вычитает оффсет хедера), our_approach (title/description/items[{title,icon?}] — иллюстрация с концентрическими кольцами и орбитами; `items` максимум 5, `icon` upload → `public/storage/pages/our_approach/icons`), interactive_equipment (title/description/items[{...}] — серии секций с видео, CTA‑формами и списками/отзывами), product_specs (tabs[{key,label,image,title,description}] — `key` is a required slug-like tab identifier (latin, no spaces); `label` is the visible tab caption), compare_models (title/description; variants supply `spec_labels` (attribute `name`) so headers stay in sync with `ProductAttribute.name`, and `is_highlighted` true variants render with an orange outline через фронтенд `CompareModels`), feature_grid (title + HTML description via RichText + items{title,description,icon,photo}/columns; `variant` plain|colored|colored-photo|advantages (default plain); colored-режим: карточки с `bg-brand-gray`, radius 20px, фиксированные отступы (pt 22 / pb 36 / px 20→30) и min-height 280/340/466 по брейкпоинтам, сетка 1→2→4 колонок и gap 15/21/19px; optional `decoration` (SVG, хранится в `public/storage/pages/feature_grid/decoration`) рендерится под контентом когда variant=colored, colored-photo или advantages; `photo` хранится в `public/storage/pages/feature_grid/photos` и при наличии полностью заменяет иконку, рендерится полноширинным изображением с radius 12–14px и фиксированной высотой 190→260px; `colored-photo` вариант использует те же фоны/радиусы, но картинка тянется на всю ширину карточки с фиксированной высотой 184→210→230px (sm→md→2xl), контентный блок имеет внутренние отступы 20→30px; `icon` — файл из админки, фронт рендерит `<img>` 70px в colored, 60px в colored-photo и 46–60px в plain; advantages-variant: фиксированная секция «Advantages» — фон/линии берутся из `decoration` (SVG upload → `public/storage/pages/feature_grid/decoration`), а иллюстрации в боковых прогалах задаются отдельными полями `decorationLeft`/`decorationRight` (upload → `public/storage/pages/feature_grid/illustrations`, показываются на `lg+`); выводит первые 6 items как 2 строки (на mobile — стек), фиксированные размеры карточек и радиусы (165/16.5 → 200/20 по брейкпоинтам), `item.icon` задаётся в админке (upload → `public/storage/pages/feature_grid/icons`) и при отсутствии просто не показывается), product_carousel (title/description/query{limit/fields/filter}), games_gallery (title + HTML description via RichText + query), **games_grid** (title/description + query{limit/fields/filter[slug|title|genre|target_age|game_type|is_indexable|video_id]}, фронт выводит плитки с категориями по `game_type`/genre, Play-оверлеем и модальным YouTube-плеером по `video_id`, кнопка “Load More” подгружает следующие страницы `/api/games`), **game_detail** (slug игры; фронт сам тянет `/api/games/{slug}`, выводит PageHeader с кастомным цветом, видео в модалке по `video_id`, жанр/target_age, ссылки на связанные продукты, описание из body/excerpt, навигация Prev/Next внутри того же жанра), news (title/description/query), stats (title/description/items{value,label}), faq (title/items{question,answer}), why_us (title/description), discount_banner (title, ctaLabel, ctaHref, icon upload → `public/storage/pages/discount_banner/icons`; фон фиксированный градиент #ff6c1c→#a2b4ff, размеры кнопки 158×53 с radius 100px, дефолтные отступы через токены контейнера: `py-16 px-4 lg:py-20 lg:px-6 2xl:py-24 2xl:px-16`, текст 38px→44px font-heading 700, gap 27/26px icon→текст и 85/77px до кнопки), cta_section (title/description/cta + textColorClass + backgroundImage), highlight_cta (title/description/cta), reviews (title/description/cta + query{limit/fields/ids/onlyActive}), **rating_summary** (title, rating, ctaLabel/ctaHref, footerText; Figma 1920/1189/360: белый фон, центрированный стек, паддинги 150px top/bottom (120px md, 96px base), заголовок `font-heading` 64/56/48/42/32 по брейкпоинтам, число рейтинга `bg-brand-gradient` 84/72/60/48, Google-глиф 46→34px, 5 звёзд `text-ui.star`, кнопка 179×53 radius 100px `bg-gradient-cta`/`shadow-cta`, отступы: заголовок→рейтинг ~72px (desktop), рейтинг→звёзды 4px, звёзды→кнопка 39px, кнопка→footerText 72px; footerText `font-heading` 20/18/16/14 `text-brand-dark/70` ширина 1051px), trusted_by (title + HTML description via RichText + footer_text + query.fields). Легаси layouts (games_list, quote_form, icon_bullets, logos, comparison_table, use_cases, reviews_feed, product_cards, social_proof) удалены из конструктора; FAQ реализован заново отдельным блоком. API отдаёт тот же формат; фронт мапит `name` → новый дизайн‑компонент. Блоки существующих страниц приведены к новому формату (hero/hero_values/feature_grid/games_gallery/stats) без легаси-полей.
  - `product_description`: добавлено поле `useGradientTitle` (boolean, default=false) — если true, заголовок рендерится как градиентный текст через `bg-brand-gradient` (`bg-clip-text` + `text-transparent`).
  - Layout **how_we_work** (frontend `HowWeWork`) — секция с заголовком + `options[{title,cards[{text}]}]`; лейбл `Option N` и нумерация карточек `01..` генерируются на фронте. Поля: `title`, `options`, `decoration` (SVG фон на `lg+`), `decorationLeft`/`decorationRight` (иллюстрации на `lg+` только при `cards.length === 6`), `padding`, `backgroundColor`. Миграция `2025_12_14_223000_add_how_we_work_to_interactive_playground_page` вставляет блок на страницу `interactive-playground` после `product_description`.
- Формы: структура хранится в `forms.config`, отправленные данные валидируются на бэкенде, сохраняются в `leads.topic` + `leads.payload`/`utm`; связь по `form_code`. `topic` берётся из запроса (например, название блока в InteractiveShowcase) либо из самой формы, и также пробрасывается в payload, если не было явного поля `topic`. Конфиг формы фронт получает на сервере Next через `GET /api/forms/{code}` (не виден в DevTools пользователя) и передает поля в клиентскую часть; в браузере выполняется только `POST /api/forms/{code}`. В админке MoonShine payload/utm показываются как таблицы key→value на детальной странице лида. Конфиг может включать `topic`, `submit_label` и `success_message` для текста кнопки/ответа.
- MoonShine / Forms: страница редактирования (`FormFormPage`) теперь использует `Rule::unique('forms','code')->ignore($id)` с корректным извлечением текущего `id`, поэтому обновление существующей формы больше не получает 422 «The Code has already been taken».
- MoonShine / Forms: в конфиге формы `config.fields[].options` (Options) используется `Json::keyValue()` с `creatable()` + `removable()`, поэтому опции можно не только добавлять, но и удалять.
- В админке MoonShine все описания/Body остаются на WYSIWYG `TinyMce` (`moonshine/tinymce`, с `->unescape()`); SEO-блоки: `SEO Title` с `->unescape()`, `SEO Description` — `Textarea` с `->unescape()` (без TinyMCE), чтобы править мета-тексты без редактора, но с многострочным вводом.
- Повторяемые JSON-поля в админке (конфиг форм, спецификации варианта, бейджи продукта, списки/фильтры блоков страницы и их вложенные коллекции) теперь рендерятся в стандартной табличной форме (горизонтально). Это убирает падение PHP 8.3 `ComponentSlot::__toString()` на PageFormPage, которое возникало из-за передачи UI-компонентов в слот при вертикальном режиме. Дополнительно, в layout `interactive_header` мы убрали `Image::multiple()` внутри Json (fixed `icon1..3`) — MoonShine сейчас не умеет рендерить `Components` коллекции в ячейке Json-таблицы, поэтому multi-upload приводил к той же ошибке.
- В админке Pages на конструкторе `Blocks` (MoonShine `Layouts` field) у каждого блока есть кнопка `Copy`, а под выпадающим `Add layout` — `Paste block`. `Copy` сериализует текущие значения блока (включая ссылки на уже загруженные медиа из `hidden_*` полей) в clipboard/localStorage; новые, ещё не сохранённые файлы браузер копировать не позволяет. `Paste` добавляет новый блок того же типа с предзаполненными данными через эндпоинт MoonShine `POST /admin/layouts-paste/{resourceUri}` (route `moonshine.layouts-field.paste`). Это позволяет быстро переиспользовать одинаковые блоки между страницами без ручного заполнения.
- Новый layout страниц `custom_software_development_intro`: секция с градиентным H2 + описанием, заголовком для feature-grid и карточками (icon/title/description). Фоновые «волны» — статический ассет `frontend/public/images/custom-software-development/waves.png`. Декорации `decorationLeft/decorationRight` отображаются только при ровно 6 items (по аналогии с FeatureGrid). Миграция `2025_12_15_000100_seed_custom_software_development_intro_block` добавляет блок на страницу `slug=custom-software-development` (после `product_description`, если он есть).
- Новый layout страниц `feature_grid_intro` (frontend `FeatureGridIntro`): секция Product Support по Figma 1920/1189/360 — градиентный H2 (84/84/38), центрированное описание (22/22/16, `text-brand-dark/70`), далее 3 белые карточки‑стата с левым выравниванием (ширина колонок через `grid-cols-3` и gap 20px; на mobile — стек с gap 10px). Карточки: `bg-white`, radius 10px, тень `0 2px 20.6px rgba(0,0,0,0.1)`, высота 215px, иконки 70/62/58px по индексам, заголовок 18px extraBold, подпись 16px. Ниже — блок “How Technical Support Works”: слева заголовок 64/64/38 и краткий текст 20/20/16, справа две карточки контактов (265×236 на 1189, 320×236 на 1920, на mobile 320×135/162), иконка телефона 24px, номер — `text-brand-sky`. Финальный RichText абзац (20/20/16) идёт ниже на всю ширину контейнера. Поля: `title`, `description` (TinyMCE/RichText), `items[{ title, description, icon, iconAlt }]`, `gridTitle` (заголовок блока поддержки), `secondaryDescription` (подзаголовок/текст слева), `secondaryItems[{ description, icon, iconAlt }]` (карточки контактов), `footerText` (TinyMCE), `padding`, `backgroundClass`, `backgroundColor`. Миграции: `2025_12_19_120000_add_feature_grid_intro_block_to_custom_software_page` вставляет блок на страницу `slug=custom-software-development` после `product_description`; `2025_12_19_120100_create_product_support_page` создаёт страницу `slug=product-support` с этим layout; `2025_12_19_130000_update_product_support_feature_grid_intro` обновляет данные блока под новые макеты.
- ProductHero на `product_landing` берет `form.code` из `Product.form` и открывает клиентский QuoteModal: конфиг формы (`/api/forms/{code}`) подтягивается на сервере Next в `(marketing)/[slug]/page.tsx` и пробрасывается в QuoteModal; в браузере выполняется только `POST /api/forms/{code}` с `topic`, `source_url` и `utm` (topic берётся из пропсов/формы).
- API форм: `GET /api/forms/{code}` отдает `{ code, title, topic, submit_label, success_message, disclaimer, fields[] }` (fields: name/label/type/required/options), `POST /api/forms/{code}` принимает значения полей + `topic`, `source_url`, `utm` JSON; дефолтные поля email/name/message отдаются, если конфиг пуст.

## Работа и дальнейшее расширение
- Для локального запуска соблюдайте контракт: проверяйте `./docker/compose.sh development ps`, далее `./docker/compose.sh development up --build` при необходимости.
- Новые схемы добавляйте миграциями; новые админские CRUD — через MoonShine resources (при генерации прописывать в `MoonShineServiceProvider` и меню `MoonShineLayout`).
- Ключевые точки расширения фронтенда: подключение новых API (pages, articles, products), использование `NEXT_PUBLIC_API_URL` для прокси через Nginx.
